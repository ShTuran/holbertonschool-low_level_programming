#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints the desired string with a newline character.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	putchar('\n');
	return (0);
}

